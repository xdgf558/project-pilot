<!--
  本文件由 Scripts/generate-executor-rules.sh 从 Docs/EXECUTOR_RULES.md 生成。
  不要直接编辑 —— 改动会在下次生成时丢失,CI 的漂移检查也会拦下。
  要改规则,改 Docs/EXECUTOR_RULES.md。
-->

你正在为 **ProjectPilot** 实现一个工作包。ProjectPilot 是一个 macOS 原生工具,
用来管理多个 GitHub 项目的开发进度,并把编码任务派给本地 AI 执行器 —— 也就是你。

下面的规则不是风格偏好。每一条背后都有一个具体的失败。

---

## 1. 开工前先读这些

1. 本工作包在任务文档里的**原文**与验收标准。不要凭工作包 ID 猜它要什么。
2. `Docs/DECISIONS.md` —— 已有的架构决策,以及每条决策付出的代价和回退条件。
   **有些看起来该做的事已经被明确否决过。**
3. 你要改的那个模块的现有测试。测试是业务规则最准确的记录。

## 2. 一次只领一个工作包

不要顺手把隔壁的问题也修了。发现了范围外的问题,写进 PR 描述的「已知限制」,
或者单独提出来,**不要塞进当前改动**。

一个 PR 对应一个工作包。基础任务紧密耦合时可以合并,但要在提交说明里写清为什么。

## 3. 在独立 worktree 和独立分支中工作

不要在主工作区改文件。不要切换主工作区的分支。

## 4. 先补测试,再写实现

新增行为必须有:

- 正向用例
- **失败用例**
- 至少一个边界用例

**并且:一个从没红过的测试等于没验证过。**

写完检查、断言或守卫之后,故意破坏它一次,确认它真的会失败、
失败信息真的说得清问题。这不是形式主义 —— Phase 0 里有两个检查
带着真 bug 通过了手工冒烟(一个正则被 `@_exported` 绕过,
一个多行块注释被误判),都是靠注入违规才发现的。

**用 `Scripts/mutation-probe.sh <文件> <原文> <替换文> [描述]` 做,不要手工。**

```
Scripts/mutation-probe.sh Sources/PilotCore/Domain/Blocker.swift \
    "    case reviewStale" "    case reviewOutdated" "改一个 code 名字"
```

退出码 0 表示改动被拦下了(**这是期望结果**),1 表示没被拦下,
2 表示探针自己没跑成。

手工做这件事出错率很高,而且**四种失效方式都会把「没拦住」误报成结论**:
锚点没命中、锚点命中了文档里的同样字样、构建失败、进程被 precondition 中止 ——
后两种确实拦下了改动,只是不长成测试失败的样子,直接 grep 会数到 0 条。
探针把这几种分开判,并强制锚点唯一。

弱断言同样等于没测。断言到具体的错误类型,不要用「抛了个错就行」——
那样错误映射改坏了测试照样绿。

断言错误类型时要**同时覆盖正交的类型**。只写「期望 A 类错误」的用例,
证明不了实现不会把所有情况都返回成 A —— 至少要有一个期望 B 类的用例
做对照。例如既有「值非法 → dataCorrupted」,也要有「类型用错 → typeMismatch」。

## 5. 不确定就停下,不要假设

遇到不确定的数据 schema、第三方 CLI 的实际行为、系统 API 的边界行为:
**先生成证据任务,不要自行假设。**

猜错要返工,猜对也常常是同一个定义写了两遍。两种结果都伤可维护性。

需要知道某个外部命令的真实行为时,**跑一遍看看**,把结果记进 `Docs/DECISIONS.md`。
Phase 0 有个现成例子:`swift test --xunit-output` 会把结果写到
`<路径去掉扩展名>-swift-testing.xml` 而不是你指定的文件名 ——
文档里没有,只能实测。

## 6. 这个项目特有的代码约束

**`PilotCore` 必须保持纯净。** 不得出现 `Process`、`FileManager`、`FileHandle`、
`Pipe`、`URLSession`、`NSXPCConnection`,不得 import 任何 UI 框架或 XPC。
它要能在没有磁盘、没有网络、没有 Git 仓库的情况下被完整测试。

**外部命令只能用 `Process.executableURL` 加参数数组。**
**禁止拼接 shell 命令字符串**,禁止 `/bin/sh -c`。这条是安全边界,不是风格。

**测试替身只放 `PilotTestSupport`。** 它不是 product,产品 target 不得依赖它。

**全部 target 使用 Swift 6 语言模式,严格并发检查开启。**
构建必须零警告 —— CI 用 `-Xswiftc -warnings-as-errors`。

**不变量检查用 `precondition`,不要用 `assert`。**
实测(Swift 6.3):`-O` 下 `assert()` 被**整个移除**,程序若无其事地继续跑;
`precondition()` 保留。SPM 的 Debug 是 `-Onone`、Release 是 `-O`,
所以写成 `assert` 的检查在发布构建里等于不存在。
`assert` 只用于纯粹开发期的、失效了也不影响正确性的自查。

**给外部系统写替身时,真假两个实现要跑同一套契约测试。**
只跟自己一致的假实现,证明不了任何关于真实环境的事。
参考 `Tests/PilotInfrastructureTests/FileSystemContractTests.swift`。

## 7. 推送前自己先跑一遍

```
swift build -Xswiftc -warnings-as-errors
Scripts/run-tests.sh debug
swift build -c release -Xswiftc -warnings-as-errors
Scripts/run-tests.sh release
Scripts/check-module-boundaries.sh
Scripts/test-check-module-boundaries.sh
Scripts/generate-executor-rules.sh --check
Scripts/test-generate-executor-rules.sh
Scripts/test-mutation-probe.sh
```

CI 跑的就是这几条,本地先跑一遍能省一个来回。

**Release 那两条不是多余的。** `-O` 下 `assert()` 会消失、优化器会暴露
Debug 看不见的警告 —— 只测 Debug 等于发布构建从来没被验证过。

新增了 test target 的话,先 `rm -rf .build` —— 否则 SPM 不会重建测试 bundle,
新套件会**静默不跑**。

## 8. 提交说明与 PR 描述怎么写

**这一节最容易写错,请认真看。**

不要写「这个函数改了 3 行」「那个参数重命名了」「新增了 5 个文件」。
**git diff 已经记录了这些,再写一遍是冗余。**

要写这三块:

1. **要解决什么问题** —— 不是「新增了 X」,而是「没有 X 会怎样」。
2. **为什么是这个方案,而不是另一个** —— 点名被否掉的备选和否掉的理由。
   只说选了什么,等于没说。
3. **边界在哪** —— 它**不做**什么、什么情况下会失效、
   哪部分被推迟以及推迟到哪个工作包。

半年后回头看这个提交,需要知道的是当时在解决什么、放弃了哪条路、为什么放弃。
**这些只存在于人脑里,不写下来就永久丢失。** 文件清单和行数是最容易写、
也最没用的部分。

PR 描述还必须包含:工作包 ID、测试证据、数据迁移影响、安全影响、
已知限制、回滚方式。

**报注入证据的数字时注明计数口径。** 「几条红」至少有两种数法:
按测试函数,还是按参数化展开后的用例。Swift Testing 的输出里
一个失败的函数会出现多行(`recorded an issue` 一行、`failed after` 一行),
直接 grep 计数会把它们重复算进去 —— 实测同一次注入,错误口径报 11,
正确口径是 3 个函数 / 6 个用例。

数字对不上会让半年后复核的人怀疑整份证据。**说不准的时候就说定性结论**
(「0 条红 → 有红」),那比一个精确但错误的数字可信。

还有一种情况根本不该报固定数字:注入之后行为本身不确定的。
例如去掉 `.sortedKeys` 会让键序随机,哪几条测试会红取决于随机顺序
是否碰巧匹配 —— 两次实测分别是 2 和 3 个函数。这种要写明它不确定。

## 9. 版本与依赖

**不使用 `latest` 之类的浮动版本。** 升级永远是主动决定,不是被动接受。

新增任何外部依赖都需要先写 ADR。当前 Swift 侧是零外部依赖,
这个状态本身有价值,打破它要有理由。

## 10. 必须停下来请人决定的情况

遇到下面任何一条,**停下,不要自行决定**:

- 自动合并
- 扩大凭据的可见范围
- 通过 shell 执行命令
- 需要 root 权限
- 引入云同步或把数据发往外部服务
- 修改本工作包范围之外的架构决策

## 11. 完成的定义

1. 代码提交到独立分支,对应测试通过。
2. 新增行为有正向、失败、边界用例,且失败用例验证过确实会红。
3. 用户可见的失败给出**可操作**的错误信息 —— 不是 "assertion failed",
   而是「哪个工具、期望什么、实际得到什么、影响哪些功能」。
4. 数据格式、命令协议或安全边界有变化时,补 ADR。
5. 日志、事件、作业文件里没有令牌、API Key、完整环境变量、用户私有文件内容。
6. 影响迁移、合并、执行器、调度或后台作业的改动,包含集成测试。

合并之后由 ProjectPilot 同步 GitHub 结果,再把任务标记为完成 ——
**PR 合并不等于任务完成**,以 GitHub 确认为准。

---

## Codex 专用

ProjectPilot 用 `--sandbox workspace-write` 和 `--ignore-user-config` 启动你。
这意味着:

- 你只能写 worktree 内的文件,写不了主工作区和用户目录。
- **用户级配置不会被加载。** 不要依赖任何本机自定义设置。
- 最终结果必须写进指定的结果文件,不能只打在 stdout 上。

保留结构化 JSON 输出的完整性 —— ProjectPilot 会按 schema 校验后才让它进入状态机,
校验不过的输出等于这次作业失败。
